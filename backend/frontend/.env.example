# Default: requests go through Vite's /api proxy during dev and preview.
API_PROXY_TARGET=http://127.0.0.1:8000

# For a separately hosted frontend, set the backend origin before building.
# Example: https://your-backend.example.com (do not append /api).
VITE_API_BASE_URL=
